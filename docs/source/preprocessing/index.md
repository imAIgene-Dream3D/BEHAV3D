# Preprocessing

Preprocessing is where every experiment starts: building the metadata, picking the output directory, and converting raw microscopy stacks into the fast-access **zarr** format that every downstream tab consumes.

## Sections

| Page | Tab in napari | What it covers |
|---|---|---|
| [Data Preparation](data_preparation) | 📋 | Metadata schema (dynamic multi-cell-type), output directory, dimension orders, Convert to Zarr |

```{toctree}
:hidden:
:maxdepth: 2

data_preparation
```
